global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "main_api"
    metrics_path: /metrics
    static_configs:
      - targets: ["main_api:8000"]

  - job_name: "reservation_api"
    metrics_path: /metrics
    static_configs:
      - targets: ["reservation_api:8001"]